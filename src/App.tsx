function App() {
  return (
    <main className="min-h-screen bg-[var(--background)] text-[var(--text-primary)] p-8">
      <h1 className="text-4xl font-bold text-[var(--primary)]">
        NomCards
      </h1>

      <button className="mt-6 rounded-xl bg-[var(--primary)] px-5 py-3 text-white transition hover:opacity-90">
        Primary Button
      </button>
    </main>
  );
}

export default App;